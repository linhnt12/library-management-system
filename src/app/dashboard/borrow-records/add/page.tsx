'use client';

import { BorrowRecordForm } from '@/components';

export default function AddBorrowRecordPage() {
  return <BorrowRecordForm submitLabel="Create Borrow Record" cancelLabel="Cancel" />;
}
