// Maximum number of renewals allowed
export const MAX_RENEWALS = 3;

// Number of days to extend when renewing
export const EXTENSION_DAYS = 14;

// Maximum total borrow period in days (from borrowDate)
export const MAX_BORROW_DAYS = 60;
